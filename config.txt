WIDTH=20
HEIGHT=15
ENTRY=0,0
EXIT=19,14
OUTPUT_FILE=maze.txt
PERFECT=True
SEED=42

if (
                    x + 1 < self.width_x # sınır duvarı mı kontrolü
                    and (x, y) not in self.locked_cells # kilitli mi diye kontrol
                    and (x + 1, y) not in self.locked_cells #duvarın diger tarafı kilitli mi diye kontrol
                    and cell.east #duvar var mı
                ):
                    closed.append((x, y, x + 1, y, "E"))
                
                if (
                    y + 1 < self.height_y
                    and (x, y) not in self.locked_cells
                    and (x, y + 1) not in self.locked_cells
                    and cell.south
                ):
                    closed.append((x, y, x, y + 1, "S"))